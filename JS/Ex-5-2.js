/* let randomizeCast = (castname) => {
    let newArr = [];
     for (let i = 0; i < castname.length; i++) {
        castMembers = castname[Math.floor(Math.random() * castname.length)]
        newArr.push(castMembers)
        
     }
     return newArr;
}
let randomized = randomizeCast(mySerie.castname)
console.log(randomized); */




/*     function randomizeCast(tvSerie) {
    tvSerie = askTvSerie()
    let tvSerieArr = tvSerie["Name of the cast members"];
    console.log(tvSerieArr);
    let newArr = [];

    for (let i = 0; i < tvSerieArr.length; i++) {
        random = Math.floor(Math.random() * tvSerieArr.length);
        newArr.push(random)
        const newMembre = tvSerieArr[random];
        console.log(tvSerieArr);
    }
    console.log(rand);   
} */