/* class Circle {
    constructor (xPos, yPos, radius) {
        this.xPos = xPos;
        this.yPos = yPos;
        this.radius = radius;
    }
    move (xOffset, yOffset) {
        this.xPos = xOffset
        this.yPos = yOffset
        console.log('New position x: ' + this.xPos + ' y: ' + this.yPos);

    }
    get surface() {
        return ("Surface is " + Math.PI * this.radius**2)
    }
}

let x = new Circle("2", "3", "8")

x.move("8", "7");
console.log(x.surface); */
