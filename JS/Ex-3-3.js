/* let ogArr = [1, 2, 3, 4, 5];
let dupArray = ogArr.slice();

console.log("Duplicate array is: ");
console.log(dupArray);

for (let elem of ogArr) {
    dupArray.push(elem);
}
console.log(dupArray); */