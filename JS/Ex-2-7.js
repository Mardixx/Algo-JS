
/* const calculator = () => {
    let n = prompt ('Give me a number of times');
    let newNumber = 0;
    

}

calculator() */
/* if (!isNaN (ask)) {
    newNumber += parseInt(ask)
    n--
}
else {
    alert('Not a number')
    if (i = 0) {
        n++
        i++
    }
    
} */

/* if (!isNaN(n)) {
    for (let i = 0; i < n; i++) {
        ask = prompt('Give me a number to addition')
        newNumber += parseInt(ask)
        if (isNaN (ask)) {
            alert(ask + ' is not a number')
            n++
            newNumber += ask
        }   
        
    }
    alert(newNumber)
    calculator()
} else {
        alert("Not a number restart function")
    newNumber = 0
    calculator()
} */