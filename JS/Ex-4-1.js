/* function add(a, b) { //Function
    return a + b;
}
console.log("The sum of those numbers is: " + add(2, 3)); */  //Sum

/* let myFunction = (a, b) => { //Arrow Function
    return a +b;
}

let myFunction = (a, b) => a + b

console.log(myFunction(2, 3)); */

/* let i = 0;

function count(i) { //Recursive
    if (i <= 100) {
        console.log(i);
        count(i + 1);
    }
}

count(i); */
/* let width = prompt("Width")
let length = prompt("Length")

function calcSurface(a, b) {
    return a * b;
}
document.write("Surface Area: " + calcSurface(width, length)); */