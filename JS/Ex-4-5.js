//Calculer la distance entre 2 points:  sqrt((X - X)² + (Y - Y)²).

/* function calcDistance(a, b, c, d) {
    let x1 = -2
    let x2 = 2
    let y1 = 2
    let y2 = -2
    let result = Math.sqrt(((x1 - x2)**2) + ((y1 - y2)**2))
    document.write(result);
}
calcDistance() */

//the calcDistance initializes 4 variable. With those, I replace in the result variable to make an operation that calculates the distance between two points.