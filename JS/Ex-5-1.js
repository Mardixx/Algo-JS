/* function askTvSerie() {
    let nom = prompt("Give me a name of a serie")
    let année = prompt("Give me a year")
    let number = prompt("Number of cast")
    let newMembre = [];
    for (let i = 0; i < number; i++) {
        let membre = prompt("Give me name of member(s)")
        newMembre.push (membre);
    }
    let myObject = {
        seriename: nom,

        prodname: année,

        castname: newMembre
    }
    
    return myObject;
}
let mySerie = askTvSerie() */